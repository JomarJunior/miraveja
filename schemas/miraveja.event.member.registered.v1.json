{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "MemberRegisteredEvent",
    "description": "Event representing the registration of a new member",
    "required": ["memberId", "email", "name", "aggregateId"],
    "properties": {
        "memberId": {
            "type": "string",
            "description": "The ID of the registered member"
        },
        "email": {
            "type": "string",
            "format": "email",
            "description": "The email address of the member"
        },
        "name": {
            "type": "string",
            "description": "The full name of the member"
        },
        "aggregateId": {
            "type": "string",
            "description": "The ID of the aggregate (same as memberId)"
        }
    }
}