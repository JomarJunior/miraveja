{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "ImageMetadataVectorIdUnassignedEvent",
    "description": "Event representing the unassignment of a vector ID from image metadata",
    "required": ["imageMetadataId", "vectorId", "aggregateId"],
    "properties": {
        "imageMetadataId": {
            "type": "string",
            "description": "The ID of the image metadata"
        },
        "vectorId": {
            "type": "string",
            "description": "The vector ID being unassigned from the image metadata"
        },
        "aggregateId": {
            "type": "string",
            "description": "The ID of the aggregate (same as imageMetadataId)"
        }
    }
}