{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "MemberActivatedEvent",
    "description": "Event representing the activation of a member",
    "required": ["memberId", "activatedAt", "aggregateId"],
    "properties": {
        "memberId": {
            "type": "string",
            "description": "The ID of the activated member"
        },
        "activatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "The timestamp when the member was activated"
        },
        "aggregateId": {
            "type": "string",
            "description": "The ID of the aggregate (same as memberId)"
        }
    }
}